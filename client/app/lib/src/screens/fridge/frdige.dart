// home screen contents
import 'package:flutter/material.dart';

class Frdige extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      color: Colors.teal,
      child: (RaisedButton(child: Text('this.title'), onPressed: null)),
    );
  }
}
